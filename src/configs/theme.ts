import { Theme } from "../types/type";

const light: Theme = {
  background: '#fff',
  accent: '#000',
  primary: '#55f244'
};

const dark: Theme = {
  background: '#000',
  accent: '#fff',
  primary: '#55f244'
};

const Themes = { light, dark }

export default Themes;
